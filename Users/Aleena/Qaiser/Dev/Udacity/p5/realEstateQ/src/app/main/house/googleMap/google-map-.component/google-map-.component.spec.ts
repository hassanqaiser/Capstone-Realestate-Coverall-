
import { ElementFinder } from 'protractor';

import { googleMap.componentComponent } from './google-map-.component.component';

describe('googleMap.component: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            this.instance = new googleMap.componentComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
