import { googleMap.component } from './google-map-.component.protractor';

describe('googleMap.component E2E', () => {

    let googlemap.component: googleMap.component = new googleMap.component;

    beforeAll(() => {
    });

    it('should check that ...', () => {
        // expect(XXX).toBeTruthy();
    });
});
